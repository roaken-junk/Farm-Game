{
  "name": "lineup-barber-queue",
  "version": "1.0.0",
  "description": "Walk-up queue for barbers: clients scan a QR code, join the line, and get an expected time.",
  "private": true,
  "main": "server/index.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning server/index.js",
    "dev": "node --no-warnings=ExperimentalWarning --watch server/index.js",
    "test": "node --no-warnings=ExperimentalWarning --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "express": "^4.21.2",
    "qrcode": "^1.5.4"
  },
  "license": "MIT"
}
